/**
 * Shared page gutter, used by both the header and the page body so they align.
 *
 * Deliberately narrow and only loosely capped: the patient table needs the
 * horizontal room, and the previous `max-w-7xl` container left large empty
 * margins on wide monitors.
 */
export const SHELL_CONTAINER = 'mx-auto w-full max-w-[1800px] px-3 sm:px-5 lg:px-6'
